<div class="post">
  <article class="card">
    <div class="card-header flex-row justify-space-between">
      <h2>{{post.title}}</h2>
      <div>
        Post by
        {{post.User.username}}
        on
        {{format_date post.createdAt}}
      </div>
    </div>
  </article>
</div>